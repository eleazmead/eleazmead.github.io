Place the wedding attire guide image in this folder.

Default configured path:

- `public/attire/wedding-attire-guide.jpg`

To use a different filename, update `APP_CONFIG.assets.attireGuideImage` in
`src/app/config/app.config.ts`.
